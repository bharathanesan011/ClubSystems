@model IEnumerable<ClubSystems.Models.MemberShip>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MemberShipType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AccountBalance)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Person)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsOverdrawn)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MemberShipType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AccountBalance)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Person.Forenames)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsOverdrawn)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MemberShipNumber">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MemberShipNumber">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MemberShipNumber">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
